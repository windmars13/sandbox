# --- Core (核心功能) ---
requests
numpy
Pillow
cryptography
lxml
beautifulsoup4
redis

# --- Utils (工具類) ---
packaging
pathspec
keyring
python-dotenv  # 補充：管理 .env

# --- CLI (命令列工具) ---
click          # 替代 docopt，現代主流

# --- Development & Testing (開發與測試) ---
pytest
pytest-cov     # 自動包含 coverage
black
isort          # 補充：讓 code 更美
flake8         # 補充：資深工程師必看的 lint
